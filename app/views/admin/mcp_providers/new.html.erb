<% content_for(:title, "New MCP Provider - Admin") %>

<div class="mb-6">
  <%= link_to "&larr; Back to MCP Providers".html_safe, admin_mcp_providers_path, class: "text-sm text-indigo-600 hover:text-indigo-900" %>
</div>

<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900">New MCP Provider</h1>
  <p class="mt-1 text-sm text-gray-500">Connect to an external MCP server</p>
</div>

<%= render "form", mcp_provider: @mcp_provider %>
