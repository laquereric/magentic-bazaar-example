<% content_for(:title, "Edit #{@mcp_provider.name} - Admin") %>

<div class="mb-6">
  <%= link_to "&larr; Back to MCP Provider".html_safe, admin_mcp_provider_path(@mcp_provider), class: "text-sm text-indigo-600 hover:text-indigo-900" %>
</div>

<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900">Edit MCP Provider</h1>
</div>

<%= render "form", mcp_provider: @mcp_provider %>
